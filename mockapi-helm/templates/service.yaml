apiVersion: v1
kind: Service
metadata:
  name: {{ include "mockapi-helm.fullname" . }}
  labels:
    {{- include "mockapi-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mockapi-helm.selectorLabels" . | nindent 4 }}
